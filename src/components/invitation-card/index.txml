 <view class="invitation-card" onTap="onTap">
    <image class="invitation-avatar" src="{{invitation.user.avatar}}"></image>
    <view class="invitation-content">
      <text class="invitation-text">{{invitation.user.name}} mời bạn ghép giỏ hàng</text>
      <text class="invatation-text invitation-text--small">{{invitation.user.message}}</text>
    </view>
  </view>