<view class="friend">
    <image class="friend-avatar" src="{{friend.avatar}}"></image>
    <view class="friend-content">
      <text class="friend-text">{{friend.name}}</text>
      <text class="friend-text friend-text--mute">{{friend.phone}}</text>
    </view>
  </view>